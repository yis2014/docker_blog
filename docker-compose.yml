version: '3.8'
services:
  blog:
    build: .
    ports:
      - "80:80"  # 容器内 Nginx 监听 80，映射到宿主机 80
    volumes:
      - /www/dk_project/wwwroot/bombcat.cc/nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always