version: '3.8'
services:
  blog:
    build: .
    ports:
      - "8080:80"  # 容器内 Nginx 监听 80，映射到宿主机 8080
    volumes:
      # 挂载宝塔里的自定义 Nginx 配置到容器内
      - /www/wwwroot/bombcat.cc/nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always